.. _device-cloud-protocol:

Device-to-Cloud Protocol
########################

The *Asset Tracker Cloud Example* uses MQTT and JSON as its main communication protocol between the device and the cloud.
This sections documents the protocol and the considerations we made when selecting it.

.. toctree::
   :titlesonly:

   Protocol.rst
   SensorDataAndConfiguration.rst